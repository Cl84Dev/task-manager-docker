Create a .env file and set these variables on task-manager-api folder.

Example:

SECRET=mysecret
SERVICE=emailservice
SERVICE_EMAIL=example@example.com
SERVICE_PASSWORD=mypassword

To start the application:

docker-compose up -d --build